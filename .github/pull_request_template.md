## What does this change?

<!-- One or two sentences: what, and why. -->

## Checklist

- [ ] `bash -n scripts/docal scripts/*.sh tests/*.sh install.sh` passes
- [ ] `shellcheck` passes (or explain why not, e.g. no network in your dev sandbox — see CONTRIBUTING.md)
- [ ] `bash tests/*.sh` passes locally
- [ ] Added/updated a test if this changes CLI behavior
- [ ] Updated `README.md` and/or `docs/*.md` if user-facing behavior changed
- [ ] Existing commands still work the same way, or the breaking change is called out explicitly below

## Breaking changes

<!-- None, or describe them. -->
